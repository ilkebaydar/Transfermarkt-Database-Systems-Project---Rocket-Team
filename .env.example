# 1. Create a copy of this file in the root directory and name it ".env"
# 2. Fill in the values below with your local MySQL configuration.
# 3. .env is added in gitignore, so our password never push on GitHub


# Database Connection Settings
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=YOUR_LOCAL_MYSQL_PASSWORD_HERE
DB_NAME=TRANSFERMARKT

# Flask Security Settings
# You can generate a random string or just write 'dev-key-123' for local development
SECRET_KEY=YOUR_SECRET_KEY_HERE